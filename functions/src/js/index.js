'use strict';

const calculation = require('./module/calculation');

const x = 1000;
const y = 20;

console.log('sum : ', calculation.sum(x, y));
console.log('subtract : ', calculation.subtract(x, y));